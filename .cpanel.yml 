---
deployment:
  tasks:
    - export NODE_ENV=production
    - source /opt/cpanel/ea-nodejs22/bin/enable
    - npm install
    - npm run build # (only if you have a build step, else remove this line)
    - pm2 restart ecosystem.config.js || node app.js 